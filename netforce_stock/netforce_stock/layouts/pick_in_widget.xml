<list model="stock.picking" action="pick_in" action_options="mode=form" priority="20">
    <head>
        <button string="New Goods Receipt" icon="plus-sign" action="pick_in" action_options="mode=form" noselect="1"/>
    </head>
    <field name="number"/>
    <field name="ref"/>
    <field name="contact_id"/>
    <field name="date"/>
</list>
