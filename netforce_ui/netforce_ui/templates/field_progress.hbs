{{#if finished}}
    <div class="progress progress-success">
        <div class="bar" style="width:100%">{{string}}</div>
    </div>
{{else}}
    <div class="progress progress-striped active">
        <div class="bar" style="width:{{percent}}%">{{string}}</div>
    </div>
{{/if}}
